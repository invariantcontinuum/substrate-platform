# Docker Environment Configuration for Substrate Platform

# ============================================
# HTTPS/HTTP Configuration
# ============================================

# Enable HTTPS mode (true/false)
# When true: HTTP redirects to HTTPS
# When false: Serves plain HTTP
ENABLE_HTTPS=false

# ============================================
# Logging Configuration
# ============================================

# Enable all logging (true/false)
ENABLE_LOGGING=true

# Enable access log (requires ENABLE_LOGGING=true)
ENABLE_ACCESS_LOG=true

# Enable error log (requires ENABLE_LOGGING=true)
ENABLE_ERROR_LOG=true

# Log level: debug, info, notice, warn, error, crit, alert, emerg
LOG_LEVEL=warn

# ============================================
# Metrics Configuration
# ============================================

# Enable metrics endpoint (true/false)
ENABLE_METRICS=true

# Port for metrics endpoint
METRICS_PORT=9113

# ============================================
# Server Configuration
# ============================================

# Server name for nginx (domain name)
SERVER_NAME=localhost

# HTTP port (inside container)
HTTP_PORT=80

# HTTPS port (inside container)
HTTPS_PORT=443

# ============================================
# SSL/TLS Configuration
# ============================================

# Path to SSL certificate (inside container)
SSL_CERTIFICATE_PATH=/etc/nginx/ssl/cert.pem

# Path to SSL private key (inside container)
SSL_CERTIFICATE_KEY_PATH=/etc/nginx/ssl/key.pem

# ============================================
# Example Configurations
# ============================================

# Development (HTTP, verbose logging, metrics enabled):
# ENABLE_HTTPS=false
# ENABLE_LOGGING=true
# ENABLE_METRICS=true
# LOG_LEVEL=info

# Production (HTTPS, moderate logging, metrics enabled):
# ENABLE_HTTPS=true
# ENABLE_LOGGING=true
# ENABLE_METRICS=true
# LOG_LEVEL=warn
# SERVER_NAME=yourdomain.com

# Minimal (HTTP only, no logging, no metrics):
# ENABLE_HTTPS=false
# ENABLE_LOGGING=false
# ENABLE_METRICS=false
